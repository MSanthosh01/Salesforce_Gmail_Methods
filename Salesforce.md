<!-- walnut:generated -->

# Salesforce — Walnut Project Context

> Auto-generated by the Walnut extension from this project's Walnut context.
> Manual edits will be overwritten on the next link/refresh. To take ownership
> of this file, delete the `walnut:generated` marker on the first line.

**Project:** Salesforce
**Repository:** Salesforce_Gmail_Methods

## Overview

# Project Overview

This project, named "Salesforce", consists of a single repository — `Salesforce_Gmail_Methods` — that implements a Gmail OTP (one-time password) extraction utility written in TypeScript and JavaScript. Based on the repository name and code structure, the toolkit appears to support Salesforce automation or end-to-end testing workflows that require programmatic retrieval of email-based verification codes.

The sole repository contains approximately 452 lines of code across two primary source files in the `walnut-methods/` directory. There is no web framework, no HTTP server, and no database. The project runs directly via Node.js (or a compatible TypeScript runner) and is structured as a scripting toolkit rather than a deployed service.

The primary external integration is the Google Gmail API via the `googleapis` npm package. The toolkit handles a full OAuth 2.0 credential flow locally — launching a browser for consent, capturing the redirect, and persisting the resulting token to disk — then uses those credentials to fetch, MIME-decode, and extract OTP strings from Gmail messages. No other external services are evident in the codebase.

## System Architecture

# System Architecture

This is a single-repo project — update once more repos complete.

**Repo: `Salesforce_Gmail_Methods`** — Role: CLI / scripting toolkit (shared utility library)

- Contains two source files under `walnut-methods/`: `generate_gmail_token.js` (OAuth handshake entry point, run once to mint and persist credentials) and `gmail_otp.ts` (library of async functions for Gmail API traversal, MIME decoding, and OTP extraction).
- No HTTP server, no API surface, and no web framework are present. The project is invoked directly via Node.js or a TypeScript runner.
- Core Node.js built-ins (`fs`, `http`, `child_process`, `path`) handle token persistence, the local OAuth redirect server, and browser launching. The `googleapis` package provides the Gmail REST API client.
- `gmail_otp.ts` exports four symbols (`readOtpFromGmail`, `runLocalOAuthFlow`, `resolveFile`, `extractTextFromPayload`) intended to be consumed by callers — likely an external test runner or automation framework — rather than run directly.
- The root `package.json` exists to declare shared dependencies so `walnut-methods` can resolve them from a single source. There are no other internal consumers visible in this repo.

## Domain

# Domain
Inferred from code only — may be inaccurate for early-stage or generic projects.

This project is a utility toolkit for automating Gmail-based OTP retrieval, most likely in support of Salesforce end-to-end testing or automation workflows. The typical use case is an automated test suite that triggers a Salesforce action requiring email verification (such as a login, password reset, or two-factor authentication step), then calls into this toolkit to programmatically read the resulting OTP from a Gmail inbox and complete the flow without human intervention.

The intended users are QA engineers or automation developers working on Salesforce test pipelines. The toolkit handles the full credential lifecycle: a one-time OAuth consent flow stores a reusable token, which subsequent runs use to authenticate against the Gmail API, locate the relevant message, decode its MIME payload, and return a bare OTP string ready for the calling test framework to consume.

## Metadata

```json
{
  "project_id": "6ab37a50a5b7b4d6b3627fa6",
  "project_name": "Salesforce",
  "generated_at": "2026-09-23T14:52:37Z",
  "llm_model": "claude-sonnet-4-6",
  "total_repos": 1,
  "total_loc": 452,
  "language_distribution": {
    "Salesforce_Gmail_Methods": 452
  },
  "primary_languages": [
    "TypeScript"
  ],
  "all_frameworks": [],
  "all_external_integrations": [],
  "repos": [
    {
      "repo_id": "github_MSanthosh01/Salesforce_Gmail_Methods_1790174080886_1skj9pnaa",
      "repo_name": "Salesforce_Gmail_Methods",
      "primary_language": "TypeScript",
      "architecture_md": "This codebase is a focused Gmail OTP extraction pipeline: it accepts OAuth credentials, connects to a live Gmail inbox via the Google API, traverses and MIME-decodes incoming messages, and returns a bare one-time-password string ready for downstream consumption.\n\nThe flow is linear. An OAuth handshake is performed first — `generate_gmail_token.js` owns credential negotiation, writing the resulting token to disk so it can be reused across runs. Once authenticated, `gmail_otp.ts` drives the full retrieval sequence: it calls the Gmail API, locates the target message, decodes the MIME payload, and extracts the OTP string. Nothing outside these two files touches the inbox or the token.\n\n**What enters, what leaves**\n\n| Stage | Input | Output |\n|---|---|---|\n| Token generation | OAuth client secrets, browser auth code | Persisted token file |\n| OTP retrieval | Token file, Gmail API response | Raw OTP string |\n\n**External dependencies**\n\n| System | Role |\n|---|---|\n| `googleapis` | Gmail REST API client — message list, fetch, decode |\n| `fs` | Token persistence (read/write credential file) |\n| `http` | Local redirect server for OAuth callback |\n| `child_process` | Launching browser for OAuth consent flow |\n| `path` / `url` | File resolution and redirect URL parsing |\n\n**Key files**\n\n- `walnut-methods/generate_gmail_token.js` — OAuth handshake entry point; run once to mint and store credentials.\n- `walnut-methods/gmail_otp.ts` — hot centre of the codebase (5 commits in 90 days, 4 exported symbols); owns API traversal, MIME decoding, and OTP extraction.\n\nThe root package exists solely to declare shared dependencies (`googleapis` and Node built-ins) so `walnut-methods` can import them from a single resolved source. There are no other consumers today.\n\n## Parts\n\n| Part | Files | Symbols | Languages |\n|---|---|---|---|\n| walnut-methods | 2 | 5 | javascript, typescript |\n| (root) | 0 | 6 |  |\n\n*(parts table rendered from the index — counts never resample)*\n\n## Architecture map\n\n```mermaid\ngraph TD\n```\n\n---\n\n# Architecture\n\nThis project is a small CLI/scripting toolkit — not a backend service or frontend app — consisting of two TypeScript/JavaScript files under the `walnut-methods/` directory. There is no detected web framework; the only declared dependency is `@playwright/mcp` (version `^0.0.82`), which suggests the scripts are designed to run as part of a browser-automation or MCP (Model Context Protocol) toolchain. The total codebase is modest at ~452 lines across 2 primary source files.\n\nExecution appears to start in two distinct ways: `generate_gmail_token.js` exposes a `main()` async function that likely runs as a standalone Node.js script to perform an OAuth token generation flow, while `gmail_otp.ts` is structured as a library of async functions (`readOtpFromGmail`, `runLocalOAuthFlow`, `resolveFile`, `extractTextFromPayload`) that are consumed by callers in the `walnut-methods` module. The module graph confirms that `walnut-methods` imports from the root level, where core Node.js modules (`child_process`, `fs`, `http`, `path`) and the `googleapis` package are the central dependencies.\n\nThere is no build pipeline or framework scaffolding evident beyond a `package.json`. The project appears to be run directly via Node.js or a compatible TypeScript runner, with no HTTP server, no database, and no API surface exposed.\n",
      "domain_md": "# Domain\nInferred from code only — may be inaccurate for early-stage or generic projects.\n\nThis project appears to be a utility toolkit for automating Gmail-based OTP (one-time password) retrieval, likely in the context of Salesforce end-to-end testing or automation workflows. The repo name \"Salesforce_Gmail_Methods\" and the central function `readOtpFromGmail` strongly suggest the intended use case: automated test flows that trigger a Salesforce action (such as a login or verification step), then programmatically read the resulting OTP from a Gmail inbox to complete the flow without human intervention.\n\nThe likely users are QA engineers or automation developers who need to handle email-based verification steps in Salesforce test suites. The `runLocalOAuthFlow` function suggests the toolkit handles Google OAuth credential negotiation locally, and `extractTextFromPayload` indicates it parses raw Gmail message payloads to extract OTP codes. The dependency on `googleapis` confirms direct integration with the Gmail API rather than a third-party email client.\n",
      "top_level_dirs": [
        "walnut-methods"
      ],
      "total_loc": 452
    }
  ]
}
```

---
_Source: Walnut code graph / project context. Treat this as background context — verify against the code before relying on it._
