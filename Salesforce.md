<!-- walnut:generated -->

# Salesforce — Walnut Project Context

> Auto-generated by the Walnut extension from this project's Walnut context.
> Manual edits will be overwritten on the next link/refresh. To take ownership
> of this file, delete the `walnut:generated` marker on the first line.

**Project:** Salesforce
**Repository:** Salesforce_Gmail_Methods

## Overview

# Project Overview

This project is a single-repo QA/test-automation utility named **Salesforce_Gmail_Methods**. Based on the repo name and code structure, it appears to be a helper toolkit used within a Salesforce end-to-end testing workflow to automate the retrieval of one-time passwords (OTPs) delivered to a Gmail inbox. The project consists of one repo with 452 lines of TypeScript and JavaScript across two source files.

The sole repo, **Salesforce_Gmail_Methods**, is a lightweight Node.js CLI/scripting toolkit with no web framework, no HTTP server, and no database layer. Its primary tech stack is TypeScript/JavaScript running on Node.js, using the `googleapis` library to interact with the Gmail API and Node's built-in modules (`fs`, `path`, `http`, `child_process`) for OAuth token management and process utilities.

The most notable external integration is Google's Gmail API via OAuth 2.0. The toolkit handles the full OAuth consent flow to obtain and persist credentials, then uses those credentials to fetch the most recent email from a specified sender and extract the embedded numeric OTP. No other external services are evident from the available facts.

## System Architecture

# System Architecture

This is a single-repo project — update once more repos complete.

- **Salesforce_Gmail_Methods** — role: CLI/scripting utility (not a backend service, frontend, or worker). It is invoked directly via Node.js rather than through a request/response lifecycle.
  - `walnut-methods/generate_gmail_token.js`: handles the one-time OAuth 2.0 setup flow — launches the Google consent page, starts a local `http` callback server to capture the authorization code, and writes the resulting token to disk via `fs`.
  - `walnut-methods/gmail_otp.ts`: the primary entry point for callers; orchestrates the end-to-end operation of authenticating with stored credentials, querying the Gmail API for the most recent message from a target sender, and returning the extracted numeric OTP.
  - The root module layer centralizes all external dependencies (`googleapis`, Node built-ins) as a single import surface consumed by the `walnut-methods` files.
  - The `WalnutBaseContext` type referenced in `gmail_otp.ts` suggests this utility is designed to be called from within a Walnut-based test automation framework, though no other repos in that framework are present in this project.

## Domain

# Domain
Inferred from code only — may be inaccurate for early-stage or generic projects.

This project automates the interception of OTP emails sent to a Gmail inbox, most likely as a supporting utility for end-to-end tests of a Salesforce application that uses email-based verification or multi-factor authentication. QA engineers or automation developers would use this toolkit to allow test suites to complete email-verification steps programmatically, without manual intervention.

The workflow has two phases: a one-time local setup phase (`generate_gmail_token.js`) where a developer authorizes Gmail API access via an interactive OAuth consent flow and persists the resulting token to disk, and a runtime phase (`gmail_otp.ts`) where automated tests supply a Gmail account identifier and a target sender address to retrieve the most recent matching OTP. The `WalnutBaseContext` type used in the runtime entry point suggests integration with a broader Walnut test automation framework, though only this single utility repo is present in the project.

## Metadata

```json
{
  "project_id": "6ab37a50a5b7b4d6b3627fa6",
  "project_name": "Salesforce",
  "generated_at": "2026-09-23T08:48:16Z",
  "llm_model": "claude-sonnet-4-6",
  "total_repos": 1,
  "total_loc": 452,
  "language_distribution": {
    "Salesforce_Gmail_Methods": 452
  },
  "primary_languages": [
    "TypeScript"
  ],
  "all_frameworks": [],
  "all_external_integrations": [],
  "repos": [
    {
      "repo_id": "github_MSanthosh01/Salesforce_Gmail_Methods_1790153175304_hhotuot6y",
      "repo_name": "Salesforce_Gmail_Methods",
      "primary_language": "TypeScript",
      "architecture_md": "This codebase is a focused Gmail OTP extraction pipeline: it accepts a Gmail account identity and a target sender address, drives the full Google OAuth flow to obtain and persist credentials, fetches the most recent matching message via the Gmail API, and returns the embedded numeric one-time code to the caller.\n\nThe **root** dependency layer owns every external boundary — `googleapis` for API access, `http` for the OAuth callback server, `fs` and `path` for token persistence, `child_process` and `url` for process and URI utilities — exposing them as a single import surface so no feature code reaches out independently. **walnut-methods** is the sole consumer: `generate_gmail_token.js` handles the OAuth lifecycle (launching the consent flow, capturing the callback, writing the token), while `gmail_otp.ts` orchestrates the end-to-end fetch-and-extract operation and is the primary entry point callers invoke.\n\n## Key facts\n\n| Concern | Detail |\n|---|---|\n| Input | Gmail account identifier + target sender address |\n| Auth | OAuth 2.0 via `googleapis`; token stored to disk by `generate_gmail_token.js` |\n| Message fetch | Gmail API, most recent message matching sender |\n| Output | Extracted numeric OTP code |\n| External systems | `googleapis`, `http`, `fs`, `path`, `url`, `child_process` |\n\n## Activity signals\n\n| File | Commits (90d) | Notes |\n|---|---|---|\n| `walnut-methods/gmail_otp.ts` | 5 | Hotspot; 4 symbols |\n| `walnut-methods/generate_gmail_token.js` | 1 | Token generation |\n\n## Parts\n\n| Part | Files | Symbols | Languages |\n|---|---|---|---|\n| walnut-methods | 2 | 5 | javascript, typescript |\n| (root) | 0 | 6 |  |\n\n*(parts table rendered from the index — counts never resample)*\n\n## Architecture map\n\n```mermaid\ngraph TD\n```\n\n---\n\n# Architecture\n\nThis project is a small CLI/scripting toolkit — not a backend service or frontend app — consisting of two TypeScript/JavaScript files under the `walnut-methods` directory. There is no detected web framework, no HTTP server, and no API surface; execution is driven by standalone async functions rather than a request/response lifecycle.\n\nThe two source files serve distinct but related purposes: `generate_gmail_token.js` contains a `main()` entry point that appears to orchestrate a one-time OAuth token generation flow, while `gmail_otp.ts` contains the core logic. The module graph shows `walnut-methods` importing from the root level, where the central symbols are Node.js built-ins (`fs`, `path`, `child_process`, `http`) and the `googleapis` library — confirming that the toolkit talks directly to Google's APIs using Node's standard library rather than any application framework.\n\nWith only 452 lines of TypeScript/JavaScript across 2 source files and no manifest-declared dependencies detected, this is a lightweight utility project. There are no database models, no HTTP endpoints, and no build pipeline evident from the facts. The likely runtime is Node.js invoked directly from the command line.\n",
      "domain_md": "# Domain\nInferred from code only — may be inaccurate for early-stage or generic projects.\n\nThis project appears to be a test-automation or QA utility that automates reading one-time passwords (OTPs) from a Gmail inbox, likely as part of an end-to-end testing workflow for a Salesforce application. The repo name \"Salesforce_Gmail_Methods\" and the central function `readOtpFromGmail` both point to this purpose: a test runner (possibly a Walnut-based automation framework, given the `WalnutBaseContext` type used throughout) needs to intercept OTP emails sent during Salesforce login or verification flows.\n\nThe likely users are QA engineers or automation developers who need their test suites to complete multi-factor or email-verification steps without manual intervention. The `runLocalOAuthFlow` function suggests the toolkit also handles the one-time setup step of authorizing Gmail API access on a developer's machine before automated runs can proceed.\n",
      "top_level_dirs": [
        "walnut-methods"
      ],
      "total_loc": 452
    }
  ]
}
```

---
_Source: Walnut code graph / project context. Treat this as background context — verify against the code before relying on it._
